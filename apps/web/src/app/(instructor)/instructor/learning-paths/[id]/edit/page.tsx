'use client';

// Instructor learning path editor - uses the same editor component as admin
// This allows instructors to edit learning paths at /instructor/learning-paths/[id]/edit
// while keeping the URL structure clean and separate from admin routes

export { default } from '@/app/admin/learning-paths/[id]/edit/page';
